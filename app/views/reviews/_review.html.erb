<li>REVIEW- <%= @review.body %>	 - <%= @review.rating %> 
	 <% if @review.user_id == current_user.id %> 
		<%#= link_to "edit", edit_@review_path(@review) %>
		<%= link_to "destroy", review_path(@review.id),method: :delete, data: {confirm: "Are you sure?"} %> 
	<% end %>
</li>